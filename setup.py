from setuptools import setup, find_packages

setup(
    name="braincache",
    version="0.1.0",
    description="A smart, pluggable and auto-cleaning cache system for Python",
    author="TonNom",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.7",
)
